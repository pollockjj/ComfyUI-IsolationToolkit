# Example Workflows

Comfy template-browser-ready workflows for `ComfyUI-IsolationToolkit`.

Format:
- `*.json` workflow template
- optional same-name `.jpg` thumbnail

These templates are copied from:
`/home/johnj/mysolate/debug/workflows/stability_battery`

They are copied intentionally so internal testing architecture remains unchanged.
